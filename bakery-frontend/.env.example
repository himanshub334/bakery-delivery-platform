# Copy this to .env.local for local development
# Leave blank — Vite proxy handles /api → localhost:8080
VITE_API_URL=

# Set this in Vercel dashboard for production, e.g.:
# VITE_API_URL=https://bakery-backend.onrender.com
